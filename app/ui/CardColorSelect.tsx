// import { useEffect, useState } from 'react';

// export default function CardColorSelect({ onSelect }: { onSelect: (color: string) => void }) {
//   const [color, setColor] = useState('#FF0000');

//   function handleChange(color: any) {
//     setColor(color.hex);
//     onSelect(color.hex);
//   }

//   return <Circle color={color} onChange={handleChange} colors={['#FF0000']} />;
// }
